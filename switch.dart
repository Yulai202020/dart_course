import 'dart:io';
void main() {
    stdout.write("Enter somethink: ");
    String? name = stdin.readLineSync();
    switch (name) {
      case "a":
        break;
      default:
        break;
    }
}